# daram-serde

`daram-serde` is currently a placeholder package in this repository.

## Current API

```daram
export fun package_name(): string
```

The implementation returns the literal package name and lives in `src/lib.dr`.
